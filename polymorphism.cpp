#include<iostream>
using namespace std;

// this property makes the sam entity such as functions and operator perform differently in different scenerio 
// polymorphism = more than one form
// it has two types 1 compile time == static binding ... can be achieved by function , operator overloading, template
// 2 run timw = dynamic binding....it can be achieved by virtuaal function and function overriding is also used 
//compiler     ... compiler and the compilation stage knows which function to execute during the program executionsv it matches the overload functions
//or operator with the number and type of parameters at compile time
// function overloading